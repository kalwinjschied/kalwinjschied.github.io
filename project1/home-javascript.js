"use strict"

let total = 0;
let count = 1;

const test1 = parseInt(prompt("To make sure you arent a robot please enter a number that can be multiplied by 2:"));
total += test1;

const multi = parseInt(total*2);

alert("Your total is: " + multi)